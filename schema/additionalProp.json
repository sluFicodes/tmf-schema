{

  "$schema": "http://json-schema.org/draft-07/schema#",

  "$id": "#ExternallyBilled",

  "title": "ExternallyBilled",

  "description": "DOME extensions for TMForum Product Offerings that are billed through a third party billing engine",

  "type": "object", 

  "properties": {

    "pricingLogicAlgorithm": {

      "type": "array",

      "items": {

        "$ref": "https://raw.githubusercontent.com/DOME-Marketplace/tmf-api/refs/heads/main/schemas/Product/PricingLogicAlgorithm.schema.json"

      },

      "description": "The PricingLogicAlgorithm entity represents an instantiation of an interface specification to external rating function (without a modeled behavior in SID). Some of the parameters of the interface definition may be already set (such as price per unit) and some may be gathered during the rating process from the event (such as call duration) or from ProductCharacteristicValues (such as assigned bandwidth)."

    },

    "relatedParty": {

      "type": "array",

      "description": "List of related parties",

      "items": {

        "$ref": "https://raw.githubusercontent.com/DOME-Marketplace/tmf-api/refs/heads/main/schemas/EngagedParty/RelatedParty.schema.json"

      }

    }

  },

  "additionalProperties": false

}
